---
- name: Install fldigi
  kewlfft.aur.aur:
    name:
      - cty
      - flxmlrpc
      - fldigi
      - flrig
  become: true
  become_user: "{{ aur.user }}"
  tags:
    - aur

- name: Install wsjtx
  kewlfft.aur.aur:
    name: wsjtx
  become: true
  become_user: "{{ aur.user }}"
  tags:
    - aur
